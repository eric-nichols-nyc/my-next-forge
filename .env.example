# ===========================================
# Shared Environment Variables (Root)
# ===========================================
#
# This file contains environment variables that are shared across multiple apps
# in the monorepo. Place your actual values in a `.env.local` file (which is git-ignored).
#
# Next.js automatically loads `.env.local` files from parent directories, so
# variables defined here will be available to all apps in apps/ and packages/.
#
# App-specific variables should still go in their respective .env.local files
# (e.g., apps/app/.env.local for app-specific configs).
#
# ===========================================

# Shared URLs (used by multiple apps)
# These URLs are shared across the monorepo and used for cross-app communication
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NEXT_PUBLIC_WEB_URL="http://localhost:3001"
NEXT_PUBLIC_API_URL="http://localhost:3002"
NEXT_PUBLIC_DOCS_URL="http://localhost:3004"
VERCEL_PROJECT_PRODUCTION_URL="http://localhost:3000"

# Database (shared across apps that need database access)
# Used by: packages/database, packages/prisma-neon
DATABASE_URL="postgresql://user:password@localhost:5432/mydb"
GOOGLE_GENERATIVE_AI_API_KEY="AI..."
# Authentication (shared across Next.js apps)
# Used by: packages/auth, apps/with-tanstack (better-auth)
# Add these to apps that need auth: apps/app/.env.local, apps/web/.env.local
# CLERK_SECRET_KEY="sk_test_..."
# BETTER_AUTH_SECRET="" # Generate with: npx @better-auth/cli secret
# NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY="pk_test_..."
# NEXT_PUBLIC_CLERK_SIGN_IN_URL="/sign-in"
# NEXT_PUBLIC_CLERK_SIGN_UP_URL="/sign-up"
# NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL="/"
# NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL="/"

# Optional: Shared service keys (uncomment as needed)
# ===========================================

# Email (Resend)
# RESEND_TOKEN="re_..."
# RESEND_FROM="noreply@yourdomain.com"

# Payments (Stripe)
# STRIPE_SECRET_KEY="sk_test_..."
# STRIPE_WEBHOOK_SECRET="whsec_..."

# Analytics
# NEXT_PUBLIC_GA_MEASUREMENT_ID="G-..."
# NEXT_PUBLIC_POSTHOG_KEY="phc_..."
# NEXT_PUBLIC_POSTHOG_HOST="https://app.posthog.com"

# Observability
# BETTERSTACK_API_KEY="..."
# BETTERSTACK_URL="..."
# NEXT_PUBLIC_SENTRY_DSN="..."
# SENTRY_ORG="..."
# SENTRY_PROJECT="..."

# Security
# ARCJET_KEY="ajkey_..."

# Feature Flags
# FLAGS_SECRET="..."

# Webhooks
# SVIX_TOKEN="..."
# CLERK_WEBHOOK_SECRET="whsec_..."

# Collaboration
# LIVEBLOCKS_SECRET="sk_..."

# Notifications
# KNOCK_API_KEY="..."
# KNOCK_SECRET_API_KEY="..."
# KNOCK_FEED_CHANNEL_ID="..."
# NEXT_PUBLIC_KNOCK_API_KEY="..."
# NEXT_PUBLIC_KNOCK_FEED_CHANNEL_ID="..."

# AI
# OPENAI_API_KEY="..."
# GEMINI_API_KEY="..."

# Storage
# BLOB_READ_WRITE_TOKEN="..."

# Rate Limiting
# UPSTASH_REDIS_REST_URL="..."
# UPSTASH_REDIS_REST_TOKEN="..."
